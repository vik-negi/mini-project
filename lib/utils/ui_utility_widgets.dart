import 'package:flutter/material.dart';

class Utils {
  Widget gapx(double x) {
    return SizedBox(
      width: x.toDouble(),
    );
  }

  Widget gapy(double x) {
    return SizedBox(
      height: x.toDouble(),
    );
  }
}
